import React from 'react'
import ChartBar from './ChartBar'
import './Chart.css'


const Chart = (props) => {
    return (
        <div>
            {props.dataPoints.map(x =>)}
        </div>
    )
};

export default Chart;
